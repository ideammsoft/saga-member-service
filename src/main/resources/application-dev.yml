#개발자 pc 기준  - dev

#Config Server 찾기
spring:
  config:
    import: "optional:configserver:http://localhost:8881"

#Eureka Server 찾기
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/